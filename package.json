{
  "name": "ilovereact",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "css": "mkdir -p bundle; yarn postcss css/app.css --watch --use autoprefixer --use postcss-import --output bundle/app.css",
    "start": "yarn lite-server",
    "clean": "rm -rf bundle",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^8.2.0",
    "lite-server": "^2.3.0",
    "postcss": "^6.0.20",
    "postcss-cli": "^5.0.0",
    "postcss-import": "^11.1.0"
  },
  "dependencies": {
    "normalize.css": "^8.0.0"
  }
}
